<div class="container">
  <h1 style="color:#0B2161"><FONT FACE="times new roman">DEFINIR PAGO</FONT></h1>
  <hr>
  <%= form_for @not_ven do |f| %>

      <div class="field">
        <td><%= f.label :pago_cod, "Método de Pago" %></td>
        <td><%= collection_select(:not_ven, :pago_cod, MetodoDePago.all, :id, :pago_tipo, prompt: true) %></td>
      </div><br>

      <div class="field">
        <td><strong>Tipo de documento</strong></td>
        <td><select name="[not_ven]doc_pago">
          <option value= 1 selected="selected">Boleta</option>
          <option value= 2 >Factura</option>
        </select></td>
      </div>

      <div class="actions"><br>
        <%= f.submit "Realizar Pago" %>
      </div>

<% end %>
  <hr>
</div>