<% flash.each do |key, value| %>
    <p> <%= value %> </p>
<% end %>
<body>
<div class="container">
  <h1 style="color:#0B2161"><FONT FACE="times new roman">ADMINISTRAR USUARIOS <small>   Editar, eliminar y crear nuevos usuarios</small></FONT></h1>
  <hr>
  <div><button type: "submit" class="btn btn-default" style="color:#FFFFFF; background-color:#2DB171;" onclick="location.href='/empleados/new';"><span class="glyphicon glyphicon-plus"></span> Crear Nuevo Usuario</button>
    <hr>
  </div>
</div>
<div class="container">
  <div class="row">
    <div class="panel panel-primary filterable">
      <div class="panel-heading" style="min-height:40px;">
        <h3 class="panel-title pull-left">Usuarios del Sistema</h3>
        <div class="pull-right">
          <button href="javascript:;" class="btn btn-default btn-xs btn-filter"><span class="glyphicon glyphicon-filter"></span> Filtrar</button>
        </div>
      </div>
      <table class="table">
        <thead>
        <tr class="filters">
          <th></th>
          <th></th>
          <th><input type="text" class="form-control" placeholder="Cargo" disabled></th>
          <th><input type="text" class="form-control" placeholder="Nombre" disabled></th>
          <th><input type="text" class="form-control" placeholder="Apellido" disabled></th>
          <th><input type="text" class="form-control" placeholder="RUT" disabled></th>
          <th><input type="text" class="form-control" placeholder="Teléfono" disabled></th>
          <th><input type="text" class="form-control" placeholder="e-mail" disabled></th>
        </tr>
        </thead>
        <tbody>
        <% @empleados.each do |d| %>
            <tr>
              <td class="td-actions">
                <%= button_to "Editar", edit_empleado_path(d), :method => :get %>
              </td>
              <td class="td-actions">
                <%= button_to "Borrar", elimUsr_path(d.emp_rut), :method => :post %>
              </td>
              <td><%= d.cargo_empleado.cargo_nom %></td>
              <td><%= d.emp_nom %></td>
              <td><%= d.emp_ape %></td>
              <td><%= d.emp_rut %></td>
              <td><%= d.emp_tel %></td>
              <td><%= d.email %></td>
              <td></td>
              <td></td>
              <td></td>
              <td></td>
              <td></td>
              <td></td>
            </tr>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
  <hr>
</div>
<div>
  <div class="row">
    <div class="col-md-1"></div>
    <div class="col-md-1">
      <button type: "submit" class="btn btn-default" style="color:#FFFFFF; background-color:#5858FA;" onclick="location.href='/admin/inicio';"><span class="glyphicon glyphicon-arrow-left"></span> Volver </button>
    </div>
  </div>
  <hr>
</div>
</body>

<div>
  <%= render 'shared/footer' %>
  <%= yield %>
</div>b</p>
