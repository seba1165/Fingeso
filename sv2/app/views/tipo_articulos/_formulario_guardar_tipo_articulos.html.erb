<%= form_for @tipoArticulo do |f| %>
    <i>Campos obligatorios (*)</i>
    <p></p>
    <div class="field">
      <%= f.label :tipo_nom, "Nombre tipo de Artículo" %><br />
      <%= f.text_field :tipo_nom, autofocus: true %>
    </div>

    <p></p>
    <div class="actions">
      <%= f.submit "Registrar" %>
    </div>
<% end %>